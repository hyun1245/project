# #의사결정 나무 모형 구축
# import seaborn as sns
# iris = sns.load_dataset('iris')
# iris.head()
# iris.info()

# iris['species'].value_counts()
# iris.describe()

# data = iris[['sepal_length', 'sepal_width', 'petal_length', 'petal_width']]
# target = iris['species']

# from sklearn.model_selection import train_test_split
# train_input, test_input, train_target, test_target = train_test_split(
#     data, target, test_size=0.2, random_state=1234)
# print(train_input.shape, test_input.shape, train_target.shape, test_target.shape)

# from sklearn.tree import DecisionTreeClassifier
# dt = DecisionTreeClassifier(random_state=1234)
# dt.fit(train_input, train_target)
# print(dt.score(train_input, train_target))
# print(dt.score(test_input, test_target))

# import matplotlib.pyplot as plt
# from sklearn.tree import plot_tree
# plt.figure(figsize = (10,7))
# plot_tree(dt, feature_names = [
#     'sepal_length', 'sepal_width', 'petal_length', 'petal_width'])
# plt.show()


# #Tree 시각화의 세부 조정, plot_tree() 함수의 매개변수(argument) 지정
# plt.figure(figsize=(10, 7))
# plot_tree(dt,max_depth=1,filled = True,
#           feature_names=['sepal_length', 'sepal_width', 'petal_length', 'petal_width'])
# plt.show()

#max_depth = 2로 지정한 tree 모형의 성능
# dt = DecisionTreeClassifier(max_depth=2, random_state=1234)
# dt.fit(train_input, train_target)  
# print(dt.score(train_input, train_target))
# print(dt.score(test_input, test_target))
 

# 불순도(impurity) 측도의 변경
# dt = DecisionTreeClassifier(max_depth = 2, criterion = 'entropy', random_state = 1234)
# dt.fit(train_input, train_target)
# print(dt.score(train_input, train_target))
# print(dt.score(test_input, test_target))

# # 가지치기(pruning)
# dt = DecisionTreeClassifier(
#     max_depth=3, criterion='gini', 
#     random_state=1234)
# dt.fit(train_input, train_target)
# print(dt.score(train_input, train_target))
# print(dt.score(test_input, test_target))

#가지치기 (max_depth=3, criterion='gini')
# plt.figure(figsize=(20, 15))
# plot_tree(dt, filled = True,
#           feature_names = ['sepal_length', 'sepal_width', 'petal_length', 'petal_width'])
# plt.show()

#변수 중요도(variable importance)
print(dt.fearture_importances_)

#새로운 inputdp 대한 예측
print(dt.predict([[4.6, 3.9, 1.5, 0.6]]))