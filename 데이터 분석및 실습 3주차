import seaborn as sns #필요한 라이브러리
import pandas as pd
import matplotlib.pyplot as plt

#아이리스 데이터셋 불러오기
df_iris = sns.load_dataset('iris') 

#데이터 구조확인 -> (관측치, 변수) + 각 변수 
print(df_iris)

#구글 colab에서 파일 업로드
# from google.colab import files 
# #업로드 창열기
# uploaded = files.upload()

#데이터 정보확인
df_iris.info() 

#앞에서 10개 데이터 확인
df_iris.head(10) 

#기술통계량 확인 -> 수치형 변수에 대해서만
df_iris.describe()

#범주형 변수에 대해서도 기술통계량 확인
#df_iris.describe(include='all')

#변수마다 결측치 개수 확인
df_iris.isnull().sum()

#결측치 존재한다면 다른값 대체(비어있는 값 = 나머지 값의 평균 값, 중앙 값 대체)
#범주형 자료 - 붓꽃의 품종 50개씩
#예) 붓꽃의 품종마다 분포의 차이를 반영하여 결측치 대체(합리적)
#붓꽃의 품종마다 어떤 값(평균, 중앙 값)으로 대체?
#그림으로 품종에 따른 분포의 차이 확인 (seaborn 라이브러리 - histogram)
df_iris['sepal_length'].fillna(
    df_iris.groupby('species')['sepal_length'].transform('mean'), inplace=True)

sns.histplot(data=df_iris, x='sepal_length', hue='species', kde=True)